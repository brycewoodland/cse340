<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>
    
    <% messages() %>

    <a href="new-classification">Add New Classification</a>
    <a href="new-inventory">Add New Vehicle</a>